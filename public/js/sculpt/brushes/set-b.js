// SPDX-License-Identifier: GPL-3.0-or-later
// Part of holosculpt. Brush set B: the pulling brushes, masking and the whole-part filters.
//
// STUB, filled in separately. Export the kernels as an array; registry.js picks them up and never
// needs editing. See registry.js for the kernel contract, and grab.js for the grab-family shape
// (restoreEachStep + anchoredOrigin + original data).
//
// Planned here: Snake Hook / Pull [snake_hook.cc], Elastic Grab [elastic_deform.cc + kelvinlet.cc],
// Thumb [thumb.cc], Nudge [draw.cc, see nudgeOffset() in draw.js], Draw Sharp [draw_sharp.cc],
// Mask plus the mask ops [mask.cc, paint_mask.cc] and the mesh filters that index.js routes to
// filters().

export default [];
